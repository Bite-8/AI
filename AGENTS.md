# Repository instructions for AI agents

1. 作業開始時にルートの `GOAL.md` を読み、現在のコード、テスト、設定、実測証拠との差分を確認する。
2. ユーザーが作業を限定していない場合は、Goalへ寄与するレビュー可能な最小単位を選ぶ。
3. 変更はbranchにまとめ、検証結果とGoalへの寄与を記載したPRを作成する。安全かつ可逆な作業に別途の着手承認を求めない。ユーザーが明示的にローカル変更だけを求めた場合は、その指示を優先する。
4. ユーザーの判断が必要な場合も、判断に依存しない安全な調査・実装は進める。PRには推奨案を差分として提示し、`Human decisions required` に質問、推奨、代替案、各案の影響を書く。
5. 未確定の質問はPRで扱う。確定後も必要な判断はコード、`configs/`、`docs/decisions/`、または`docs/experiments/`へ反映し、PRの会話だけに残さない。
6. 有料資源、外部システム、公開、削除に関する操作は、PRのmergeから許可を推測しない。対象、費用・時間上限、停止・復旧方法を提示し、操作そのものへの明示的な承認を得る。
7. Goal、Success Criteria、Non-goals、完了条件を独断で変更または緩和しない。それらを変える提案は独立したPRにし、ユーザーの判断を求める。
